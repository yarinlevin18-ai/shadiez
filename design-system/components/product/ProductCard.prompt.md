Photography-led catalog card — image on top, calm text block below. Hover lift + ken-burns zoom when `onClick` is set.

```jsx
<ProductCard
  image="assets/kits/kit-cream.jpg"
  eyebrow="The line"
  name="Cream Shade + Tote"
  meta="Walnut frame · cream canvas"
  footer={<Button variant="warm" size="sm">Shop Cream</Button>}
/>
```

Near-square corners, warm hairline border, soft shadow. Keep copy spare — let the photo lead.
