Single-line text input. Pair with `Field` for a label, or use bare.

```jsx
<Input placeholder="you@email.com" type="email" />
<Input invalid defaultValue="not-an-email" />
```

Matches the lead-dialog inputs: 46px tall, warm hairline border, navy focus ring, 4px corners. Set `invalid` to show the error state.
